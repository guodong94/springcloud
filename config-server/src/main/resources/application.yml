server:
  port: 8200

spring:
  application:
    name: config-server
#spring.cloud.config
  cloud:
    config:
      server:
        label: master
        git:
          uri: https://github.com/guodong94/SpringcloudConfig
          searchPaths: respo
          username:
          password:

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8100/eureka/







